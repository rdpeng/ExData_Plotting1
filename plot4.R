power<- read.table("household_power_consumption.txt", sep= ';', header = TRUE, stringsAsFactors = FALSE)
power$Date<- strptime(power$Date, "%d/%m/%Y")
power$Date<- as.Date(as.character(power$Date))
install.packages("dplyr")
library(dplyr)
power1<- filter(power, Date == "2007-02-01" | Date == "2007-02-02")
par(mfrow = c(2,2), mar = c(4,4,2,1))
with(power1, plot(dat, as.numeric(Global_active_power), type = "l", ylab = "Global Active Power", xlab = " "))
with(power1, plot(dat, as.numeric(Voltage), type = "l", ylab = "Voltage", xlab = "datetime"))
with(power1, plot(dat, as.numeric(Sub_metering_1), type = "l", xlab = " ", ylab = "Energy sub metering"))
with(power1, lines(dat, as.numeric(Sub_metering_2), col="red"))
with(power1, lines(dat, as.numeric(Sub_metering_3), col="blue"))
legend("topright", c("Sub_metering_1", "Sub_metering_2", "Sub_metering_3"), bty = "n", col = c("black", "red", "blue"), lty = 1, cex = 0.5)
with(power1, plot(dat, as.numeric(Global_reactive_power), type = "l", ylab = "Global reactive power", xlab = "datetime"))
dev.copy(png, 'plot4.png')
dev.off()
